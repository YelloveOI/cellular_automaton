#include <iostream>
#include "SFML/Graphics.hpp"

int main() {
    const unsigned short tile_size = 5;
    const float interval = 0;
    float time = 0;
    sf::Clock clock;
    sf::RectangleShape black_tile(sf::Vector2f(tile_size, tile_size));
    black_tile.setFillColor(sf::Color::Black);
    sf::RenderWindow(
            sf::VideoMode(tile_size*_)
            )
}
